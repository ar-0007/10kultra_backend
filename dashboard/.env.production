# Baked into every production build so a Netlify deploy is correct even if nobody sets an env var.
# Not a secret — it is a public URL, and the backend is what enforces auth and CORS.
# Netlify → Site configuration → Environment variables → VITE_API_URL overrides this.
VITE_API_URL=https://tenkultra-backend.onrender.com
