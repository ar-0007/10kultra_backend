# Netlify config for the 10K Ultra dashboard (Vite static SPA).
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every route serves index.html so React Router handles it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
